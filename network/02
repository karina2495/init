#Определите и отобразите характеристики интерфейса Ethernet:
#(a)Определить широковещательный адрес

ifconfig en0 | grep broadcast | awk '{print $6}'

#(b)Выводит все IP-адреса которые являются частью одной подсети

arp -a
