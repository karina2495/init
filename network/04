#Определить шлюз по умолчанию в таблице маршрутизации

netstat -r -n | grep default | head -1 | awk '{print $2}'

#-n показывает сетевые адреса в виде чисел (иначе отображает символически)
#-r показывает таблицы маршрутизации
#head -1 удаляет одну строку из результата grep
